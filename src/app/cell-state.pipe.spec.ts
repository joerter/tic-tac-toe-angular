import { CellStatePipe } from './cell-state.pipe';

describe('CellStatePipe', () => {
  it('create an instance', () => {
    const pipe = new CellStatePipe();
    expect(pipe).toBeTruthy();
  });
});
